
// tmp_test_supabase.ts — disabled to avoid build/typecheck failures in CI.
// This file previously imported `dotenv` which isn't available in the build environment.
// If you need to run a local supabase test, recreate this file locally outside the Next build.

export {};
